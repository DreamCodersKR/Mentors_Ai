# 필요한 python 라이브러리 목록
# pull 받으면 각자 로컬에서 가상환경 만들어야함 -> python -m venv mentors_env

1. python -m pip install --upgrade pip => 파이썬 패키지 최신버전으로 업그레이드하기
2. pip install flask => 플라스크 설치
3. 가상환경내 라이브러리 txt로 뽑아내는법 -> pip freeze > requirements.txt